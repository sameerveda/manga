apply plugin: 'java-library'
apply from: '../base.gradle'

version = 0.15

repositories {
    jcenter()
}

dependencies {
    compileOnly(MY_UTILS)
    implementation(JSOUP, FEATHER, ORG_JSON, project(':api-models-helpers'))
}
